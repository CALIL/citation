# 変更履歴

抽出結果が変わる変更は、同じダンプを処理しても以前と違う出力になる。
過去のデータと比較する場合はバージョンを揃えること。

## 2.0.0

抽出精度を見直し、単一スクリプトからパッケージへ再構成した。
**出力の内容が大きく変わる**ため、メジャーバージョンを上げている。

同じダンプを処理したときの件数の変化:

| ダンプ | 1.0.0 | 2.0.0 |
|---|---:|---:|
| jawiki-20240401 | 1,073,563 | 1,042,239 |
| jawiki-20250601 | 1,175,404 | 1,140,156 |
| enwiki-20240401 | 7,023,140 | 5,295,568 |
| enwiki-20250601 | 9,212,634 | 5,722,443 |

英語版の減りが大きいのは、記事以外の名前空間（20250601で139万件）と、
978を補うと成立するだけの数字列の誤検出が多かったため。出典と判定された
レコードの割合は37.8%から61.4%に上がっている。

### 誤検出を減らした変更

- **記事以外の名前空間を対象外にした**。`<ns>` を見て `Wikipedia:` や `Template:`
  などを読み飛ばす。jawikiで10,914件、enwikiで139万件が該当した
- **979で始まるISBN-13が別のISBN-10に化けるのを直した**。ISBN-13の判定条件に979が
  無く、`979-0014006723` が `0014006723` として出力されていた
- **978を補うと成立するだけの数字列を採用しないようにした**。ISBN-13のチェック
  デジットは10通りしかないため任意の10桁数字列の約1割が該当する。jawikiで49,120件が
  除外され、その大半は日付・電話番号・背番号の羅列だった
- **接頭辞の無いスペース混じりの数字列を採用しないようにした**。`2008-12-10 00`
  （日付と時刻）や `10115 - 14199`（郵便番号の範囲）を拾っていた。1,871件が該当

### 取りこぼしを拾えるようにした変更

- **citeテンプレートを種類によらず出典として扱う**。`{Cite book` としか比較して
  おらず、小文字の `{{cite book`（jawikiで約21%）や `{{cite journal` を取りこぼしていた
- **著作一覧の見出し辞書を拡張した**。日本語18語（著書・既刊一覧・著作ほか）、
  英語4語（Collected editions・Published works ほか）を追加。英語版の出典見出し
  （References・Bibliography ほか）も辞書に無く、142万件がブーストされていなかった
- **日本語版固有のテンプレート記法を接頭辞として認識する**。`{{ISBN2|...}}` が
  226,457件使われているのに認識しておらず、15,970件を取りこぼしていた
- **書店や書誌サイトのURLに含まれるISBNを拾う**。Amazonの `/dp/`、版元ドットコムや
  WorldCatの `/isbn/`、紀伊國屋書店の `ISBN=`、CALILの `calil.jp/book/` に対応
- **ISBNの区切り位置でハイフンが入っていれば接頭辞が無くても採用する**。
  `0-520-20743-2` のような英語圏のISBNは先頭が4でないためスコアが足りなかった。6,463件
- **978で始まるISBN-10が空文字になるのを直した**。isbnlibの `to_isbn10()` が先頭
  3文字だけでISBN-13と誤認するため。空のISBNは0件になった
- **到達不能だった16桁ISBNの救済分岐を機能させた**

### 出力の見た目に関する変更

- **タイトルと見出しのXMLエスケープを解除した**。`ゲーム&amp;ウォッチ` のように
  記事名が壊れており、記事名で引くときにマッチしなかった。4,322件を修正
- **見出しが途中で切れる問題を直した**。`== A <span class="x"></span> ==` のように
  `=` を含む見出しが切れていた。enwikiで35,758件が該当
- **スコアの浮動小数点誤差を丸めた**。`0.8999999999999999` のような値が出ていた
- **出力の改行をLFに固定した**。Windowsで実行するとCRLFになっていた

### スコアの調整

- **著者が書かれているかをスコアに反映する**。出典として挙げる文献には著者を書くが、
  作品一覧では記事の主題そのものなので省略される（出典44.6%に対し作品一覧4.0%）

### 機能の追加

- **multistreamダンプの並列処理**。ストリーム単位で分割して処理する。
  jawiki 4.7GBが25分36秒から1分30秒に、enwiki 23GBは13分で終わる。
  出力は逐次処理と1バイトも変わらない
- **`--unique`**。同じページに同じISBNが複数あれば1件にまとめる。
  jawikiでは9%が重複しており、指定すると出力が9.3%小さくなる
- **`citation-audit`**。抽出結果の集計（`stats`）と2つの出力の比較（`diff`）を行う
- **cgroupのCPU制限に従う**。KubernetesやDockerで並列数が制限を超えないようにする

### 内部の変更

- 単一スクリプト（173行の関数1つ）から `src/citation/` のパッケージへ再構成
- テストを180件追加。ISBN正規化の12パターンを網羅している
- GitHub Actionsでruffとpytestを実行
- 依存から halo と colorama を削除（click と isbnlib のみ）
- 未対応の問題を [KNOWN_ISSUES.md](KNOWN_ISSUES.md) に記録

## 1.0.0

単一スクリプト `citation.py` として運用していた時期のバージョン。
