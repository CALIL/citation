# 既知の問題

抽出精度と実装に関する問題を、個別に判断・着手できる形で列挙する。未対応のものは
**現行の挙動のまま残してある**。挙動を変える修正は影響を測った上で採否を決める。

## 影響の測り方

```bash
# 修正前後の出力を比較する
uv run citation-audit diff before.jsonl after.jsonl

# 単体の集計（空ISBN件数、スコア分布、見出しの内訳）
uv run citation-audit stats citation-jawiki-20260401.jsonl
```

小さいケースは `tests/golden/synthetic.jsonl` の差分でも確認できる。
このファイルは現行の挙動を1件ずつ固定したもので、修正を入れると差分として現れる。

jawiki-20260401ダンプ（4.7GB / 3,069,508ページ）での基準値:

| 項目 | 値 |
|---|---:|
| 抽出件数 | 1,237,402 |
| 空のISBN | 0 |
| 処理時間（28並列） | 約1分30秒 |

---

# 未対応の問題

## 4. 出力ファイルを確認なく上書きする 【対応不要と判断】

- **症状**: 既存ファイルを指定しても警告なく上書きされる
- **箇所**: `src/citation/cli.py`。`click.Path(exists=False)` は「存在しなくてもよい」と
  いう意味で、存在チェックはしていない
- **判断**: 利用者の判断で対応不要とした。UNIXのコマンドとしては上書きが自然な挙動であり、
  処理時間も並列化で1分半に短縮されたため、やり直しのコストが小さい

## 7. 見出しにウィキマークアップと非XMLの実体参照が残る

- **症状**: `h1='{{math|&minus;1}} の平方根'` のようにテンプレート記法がそのまま残る。
  また `&nbsp;` や `&ndash;` のようにウィキテキスト側に書かれた実体参照も残る
- **箇所**: `src/citation/headings.py` の `parse_heading()`
- **実測**: jawiki-20260401 で実体参照が残るのは58件（`&ndash;` 21 / `&nbsp;` 19 / `&amp;` 7 ほか）。
  テンプレート記法の件数は未計測
- **背景**: XMLのエスケープ解除（済15）は済んでいる。残っているのはウィキテキストの記法で、
  解釈するにはマークアップのパースが必要になる
- **修正案**: 見出しから `{{...}}` や `[[...]]` を取り除き、`&nbsp;` などをもう一段
  アンエスケープする。ただし見出しの意味を変えてしまわないか要検討

---

# 対応済みの問題

判断の経緯を残すために記録している。いずれもjawiki-20260401の全件で影響を測定した。

| # | 問題 | 対応 | 影響 |
|---|---|---|---|
| 済1 | ISBN表記のない行を飛ばすガードの論理が反転 | 条件ごと削除 | +3件（誤除外の解消）。高速化効果は無かった |
| 済2 | `{{cite book`（小文字）等を出典と判定できない | 正規表現で表記ゆれを吸収し `{{Citation}}` も対象に | 76,489件のスコア補正、`is_ref` 変化9,107件 |
| 済3 | 記事以外の名前空間が混入 | `<ns>` を見て記事以外を読み飛ばす | -10,914件（すべて記事以外） |
| 済4 | 978で始まるISBN-10が空文字になる | `is_isbn10()` で先に確かめるラッパーを追加 | 空のISBNが19件から0件に |
| 済5 | 16桁ISBNの救済分岐が到達不能 | `isbn[3:16]` に修正しelifチェーンへ統合 | 出力に変化なし（実データに該当なし） |
| 済6 | スコアの浮動小数点誤差 | `round(score, 1)` | 671件が `0.8999999999999999` から `0.9` に |
| 済7 | 採用条件と出力スコアの不整合 | 実装は変えず説明を修正 | 出力に変化なし |
| 済8 | 出典セクションの見出し辞書が日本語のみ | 英語の見出しを追加し大文字小文字を無視 | 英語版で出典判定率が86.9%から93.7%に |
| 済9 | 出力の改行が実行環境依存 | `newline="\n"` を指定 | 常にLF。内容は不変 |
| 済10 | 979で始まるISBN-13が別のISBN-10に化ける | ISBN-13の判定条件を `is_isbn13()` に委ね、979はISBN-13のまま出力 | 誤変換24件を解消し、取りこぼしていた648件を追加 |
| 済11 | 出典と判定するciteテンプレートが book と citation だけ | 種類を問わずすべてのciteテンプレートを対象に | 6,418件の判定が変わり、出典と判定が1,071件増えた |
| 済12 | 同じページに同じISBNが何度も出力される | `--unique` オプションを追加（既定は従来どおり） | 指定時は112,120件が除去され出力が9.3%小さくなる |
| 済13 | `--jobs` の値を検証していない | `click.IntRange(min=1)` に変更 | 0や負数が明確なエラーになる |
| 済14 | `citation-audit diff` のメモリ消費 | 行のハッシュで差分を求め、該当行だけ読み直す2パス方式に | 124万件×2で0.75GiBから0.16GiBに |
| 済15 | タイトルと見出しにXMLの実体参照が残る | `html.unescape()` を適用 | 4,322件の title / h1 / h2 を修正 |
| 済16 | 接頭辞なしのスペース混じり数字列を誤検出 | 該当パターンを候補から除外 | 日付やページ範囲など1,871件の誤検出を除去 |
| 済17 | 著作一覧の見出し辞書が「作品」「作品リスト」だけ | 実データの見出し分布から18語を追加し、英語の出典見出しも3語補った | 165,939件の判定が変わり、出典と判定が59,764件減った |
| 済19 | 出典か作品一覧かを見出しだけで判断していた | citeテンプレートの著者パラメータの有無をスコアに反映 | 444,041件のスコアが+0.5された |
| 済18 | `{{ISBN2\|...}}` などのテンプレート記法を接頭辞と認識しない | 接頭辞リストにテンプレート記法を追加 | 236,164件が接頭辞ありと判定され、15,970件が新たに採用された |

## 判断の記録

- **済1で「意図どおりに直す」を選ばなかった理由**: 論理を正すと処理は10倍速くなる
  （実ダンプで1分26秒→52.8秒）が、接頭辞のない裸のISBNが62,760件（5.1%）失われる。
  これはREADMEに明記された仕様なので、速度のために検出を捨てるのは本末転倒と判断した
- **済7で再判定しなかった理由**: スコアが1.0を下回る12,614件は「著作一覧に載っている本」で
  ISBN自体は有効。`is_ref` で区別できるため、捨てるより残すほうが利用者に有益と判断した
- **済10で `isbn` にISBN-13を入れた理由**: 979には対応するISBN-10が原理的に存在しない。
  空文字にすると情報が失われ、採用しないと672件を取りこぼす。13桁のまま入れれば
  ISBNからの検索に使えるため、READMEの説明を実態に合わせるほうを選んだ
- **済16でスペースを一律に禁止しなかった理由**: 正規表現からスペースを外せば誤検出は
  消えるが、`978 3 406 63951 7` のような本物のスペース区切りISBNを3,017件失う。
  接頭辞かISBN-13のプレフィックスがあるものは残す条件にしたところ、実測でも
  誤検出1,871件だけが消え、内容が変わったレコードは0件だった

## スコア精度の改善で試して使えなかったもの

誤検出を減らす指標を探して実データを調べた記録。いずれも判別力が足りなかった。

| 試した指標 | 結果 |
|---|---|
| `isbnlib.mask()` による出版社記号の検証 | 1,239,273件のうち弾けたのは56件（0.005%）。`0000020666` のようなゼロ埋めも `0-00-002066-6` として通る |
| 元表記にハイフンがあるか | スコアが高いほどハイフン率が低いという逆相関（3.0で52.5%、0.5で93.3%）。加点の根拠にならない |
| 同じISBNが何ページで参照されているか | 2ページ以上で参照されるのは16.8%のみ。実在の専門書でも1ページが普通で、ストリーミング処理とも相性が悪い |
| セクション内のISBN密度 | 作品一覧は中央値4〜6、出典は1〜2と差はあるが分布が重なる。閾値を変えても適合率は33%以下 |
| 行がリスト記法か | 出典88.0%、作品一覧91.2%でほぼ差がない |
| ページのカテゴリやInfobox | 「存命人物」はカバー率45%だが適合率53%。Infoboxは比率が大きくてもカバー率が最大6%。そもそも1つのページに「参考文献」と「著書」が同居するため、ページ単位の判定ではセクションの差を区別できない |
