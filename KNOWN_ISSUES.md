既知の問題
==========

抽出精度と出力に関する未対応の問題を、個別に判断・着手できる形で列挙する。
いずれも**現行の挙動のまま残してある**。修正するとどれも出力が変わるため、
影響を測った上で採否を決める。

影響の測り方
----

```bash
# 修正前後の出力を比較する
uv run citation-audit diff before.jsonl after.jsonl

# 単体の集計（空ISBN件数、スコア分布、見出しの内訳）
uv run citation-audit stats citation-jawiki-20260401.jsonl
```

小さいケースは `tests/golden/synthetic.jsonl` の差分でも確認できる。
このファイルは現行の挙動を1件ずつ固定したもので、修正を入れると差分として現れる。

現行実装での基準値（jawiki-20260401ダンプ / 4.7GB）:

| 項目 | 値 |
|---|---:|
| ページ数 | 3,069,508 |
| 抽出件数 | 1,249,536 |
| 除外件数 | 15,530,430 |

出力のSHA-256（改行をLFに正規化した上での値）は
`tests/golden/baseline-jawiki-20260401.sha256` に記録してある。

---

1. ISBN表記のない行を読み飛ばすガードが機能していない
----

- **症状**: 高速化のための条件が1件も除外していない
- **箇所**: `src/citation/extract.py` の `Extractor._scan()`
- **原因**: `ISBN` と `isbn` と `Isbn` の3つ**すべて**を含む行を読み飛ばす条件になっており、
  意図（いずれも含まない行を飛ばす）と論理が逆。`and` を `== -1` の連結にすべきところ
- **実測**: jawikiダンプの先頭60万行でヒット0件
- **修正案**: 3つとも含まない行を読み飛ばす条件に直す
- **想定される影響**: 接頭辞なしの裸の数字列（例: 参考文献欄に数字だけ書かれたISBN）が
  拾われなくなるため、件数は**減る**方向。現在の出力には接頭辞なしで採用されている
  レコードが相当数あるので、影響は小さくない

2. `{{cite book`（小文字）を出典と判定できない 【対応済み】
----

- **症状**: cite系テンプレートの2割前後を出典として認識できていなかった
- **箇所**: `src/citation/extract.py` の `CITE_TEMPLATE_RE`
- **原因**: `"{Cite book"` としか比較しておらず、大文字小文字と空白の揺れを吸収していなかった
- **修正前の実測**: jawikiダンプ先頭60万行のcite系4,829件のうち、`{{cite book` 979件 /
  `{{Cite Book` 32件 / `{{ cite book` 2件 が取りこぼし（約21%）
- **対応**: `\{\s*(?:cite\s*book|citation\s*[|}])` を大文字小文字を無視して判定する。
  波括弧を1つ以上としているのは、変更前の `"{Cite book"` という部分一致の挙動を
  包含するため。`{{Citation}}` も出典テンプレートなので対象に含めたが、出典が**無い**
  ことを示す `{{Citation needed}}` と字面が紛らわしいので、引数の区切りか閉じ括弧が
  直後に来る場合だけを拾って区別している
- **変更による影響**: jawiki-20260401（1,249,536件）で計測した。レコード数と抽出される
  ISBNそのものは変わらず、出典としての評価だけが変わる

  | 対応内容 | スコアが+0.5されたレコード | `is_ref` が false → true |
  |---|---:|---:|
  | cite bookの表記ゆれを吸収 | 44,265 | 7,823 |
  | `{{Citation}}` を対象に追加 | 32,224 | 1,284 |
  | 合計 | 76,489（全体の6.1%） | 9,107 |

  スコアが上がったレコードの大半は「参考文献」などの見出しによってすでに `is_ref` が
  `true` だったもので、スコアだけが上がっている。出典と判定されたレコードは
  889,069件から898,176件になった

3. 記事以外の名前空間が抽出対象に混ざっている 【対応済み】
----

- **症状**: `Wikipedia:`、`Help:`、`Portal:`、`Template:` などのページから抽出したISBNが
  記事のものと区別なく出力される
- **箇所**: `src/citation/extract.py` の `Extractor._scan()`
- **修正前の実測**: jawikiダンプ先頭60万行で、記事1,831ページに対し `ns` が0以外のページが67件
- **対応**: タイトルの直後に現れる `<ns>` を読み、記事（ns=0）以外はページごと読み飛ばす。
  `<ns>` を持たない古い形式のダンプでは行が現れないため、従来どおり全ページが対象になる
- **変更による影響**: jawiki-20260401 で10,914件（0.87%）が出力されなくなった。
  除外された4,091ページの内訳は次のとおりで、記事は1ページも含まれていない

  | 名前空間 | ページ数 |
  |---|---:|
  | Wikipedia: | 3,340 |
  | Template: | 506 |
  | プロジェクト: | 66 |
  | Portal: | 63 |
  | Category: | 46 |
  | モジュール: | 34 |
  | ファイル: | 29 |
  | Help: | 6 |
  | MediaWiki: | 1 |

  読み飛ばすぶん走査量が減るため、処理時間も1分34秒から1分26秒に短くなった

4. `to_isbn10()` が 978 で始まるISBN-10を変換できない 【対応済み】
----

- **症状**: `"isbn": ""` という空のISBNが出力に混ざる
- **箇所**: `src/citation/isbn.py` の `to_isbn10()`
- **原因**: isbnlibの `to_isbn10()` は先頭3文字が `978` かどうかだけでISBN-13と判断する。
  `9784062577` のように**978で始まる10桁の正当なISBN-10**を渡すと、ISBN-13として
  検証に失敗し空文字を返す
- **修正前の実測**: citation-jawiki-20250601.jsonl に16件
- **対応**: 先に `isbnlib.is_isbn10()` で確かめ、すでにISBN-10ならそのまま使う
  ラッパーを `citation.isbn.to_isbn10()` として用意した
- **変更による影響**: jawiki-20260401 で空のISBNが解消した。レコード数は変わらない

  | 項目 | 変化 |
  |---|---:|
  | 空のISBN | 19 → 0 |
  | ユニークISBN | 673,807 → 673,822 |
  | 内容が変わったレコード | 19 |

5. `I13(978978Cut)` の分岐が到達不能
----

- **症状**: 「978が二重に付いた16桁」を救う分岐が一度も発火しない
- **箇所**: `src/citation/isbn.py` の `normalize_isbn()`
- **原因**: `isbn[6:16]` は必ず10桁になるのに、`isbnlib.is_isbn13()` は13桁を要求するため
  条件が成立しない。おそらく `isbn[3:16]` の誤り
- **実測**: 構造上0件（`tests/test_isbn.py::test_978が二重の16桁は判定できない` で固定）
- **修正案**: `isbn[3:16]` に直す。ただし項目4と合わせて、二重978がそもそもどう発生するかを
  確認してから決める
- **想定される影響**: 現在 `?` として除外されている16桁の候補が採用されるようになる

6. スコアに浮動小数点の誤差がそのまま出る
----

- **症状**: `"score": 0.8999999999999999` のような値が出力される
- **箇所**: `src/citation/extract.py` の `Extractor._build_record()`
- **原因**: 0.9 / 0.5 / 1.0 を順に足し引きしているため、二進小数で表せない端数が残る
- **実測**: citation-enwiki-20250601.jsonl の先頭40万件中105件
- **修正案**: 出力時に `round(score, 1)` する。あるいは内部を整数（10倍値）で持つ
- **想定される影響**: 値としては誤差の範囲だが、出力の見た目と文字列一致が変わる

7. 採用条件のスコアと出力されるスコアが違う
----

- **症状**: 「スコア1.0以上を採用」と説明しているのに、`score` が1.0未満のレコードが出力される
- **箇所**: `src/citation/extract.py`。採用判定の後に見出しによる減点を適用している
- **実測**: citation-jawiki-20250601.jsonl の先頭40万件中2,767件（0.5が2,662件、0.9が105件）
- **修正案**: 減点後に再判定するか、READMEの説明を実態に合わせる。どちらが正しい仕様かは
  利用側の期待による
- **想定される影響**: 再判定する場合は件数が減る。説明を直すだけなら出力は変わらない

8. 出典セクションの見出し辞書が日本語しかない 【対応済み】
----

- **症状**: 英語版Wikipediaで出典セクションのISBNがブーストされない
- **箇所**: `src/citation/headings.py` の `REF_HEADINGS` と `NON_REF_HEADINGS`
- **修正前の実測**: citation-enwiki-20250601.jsonl の先頭40万件で、`References` 60,752件 /
  `Further reading` 29,725件 / `Bibliography` 22,473件 / `Sources` 13,701件がすべて
  辞書に無く、`<ref>` タグのある行だけが出典として拾えている状態だった
- **対応**: 英語の出典見出し（references, bibliography, further reading, sources,
  notes, footnotes, citations, works cited など）と、著作一覧の見出し（works,
  publications, selected works）を辞書に追加した。英語は "Further reading" と
  "Further Reading" のように大文字小文字が揺れるため、辞書を小文字で持ち、
  比較するときに見出し側を小文字化する方式にした
- **変更による影響**: レコード数は変わらず、出典としての評価だけが変わる

  | ダンプ | 内容が変わったレコード | `is_ref` が false → true |
  |---|---:|---:|
  | jawiki-20260401（1,249,536件） | 227 | 104 |
  | enwiki-20250601の先頭49,965ページ（340,851件） | 114,973（33.7%） | 23,205 |

  英語版では出典と判定されたレコードの割合が86.9%から93.7%に上がった。
  日本語版で影響が小さいのは、英語の見出しがほとんど現れないため

9. 出力の改行コードが実行環境に依存する
----

- **症状**: Windowsで実行するとCRLF、Linux/macOSではLFのJSONLが出力される
- **箇所**: `src/citation/cli.py` の出力ファイルオープン。`newline` を指定していない
- **修正案**: `open(..., newline="\n")` を指定して常にLFにする
- **想定される影響**: 中身は同じだがファイルのバイト列が変わる。公開済みデータとの
  ハッシュ比較には影響する。テスト側は改行を正規化して比較しているため無関係
