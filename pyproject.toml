[project]
name = "citation"
version = "1.0.0"
description = ""
authors = [{ name = "CALIL Inc." }]
requires-python = ">=3.14"
dependencies = [
    "click",
    "isbnlib",
    "colorama",
    "halo",
]

[tool.uv]
package = false